"""BilimAI — word verifier for dictation marks (E5.8, 2026-08-19): the CTC judge in the product.

    v = CTCWordVerifier()                                  # models/readingpipeline/ocr (MIT), CPU ONNX, ~30 ms/word
    out = v.judge(img, [{"bbox": ink_word_box, "key": "проявить", "read": "проевить"}, ...])
    out[i] -> {"margin": float, "best": "проевить", "verdict": "error" | "review" | "ok"}

Why: the GLM reader decodes with a language prior and cannot tell "the pupil misspelled" from "I misread"; the CTC word
reader has no prior (one letter per image column). For a word whose read differs from the key, we score the key spelling
and ~500 one-letter variants (bilimai.dictation.candidates wide) on the ink alone; margin = best variant − key. High
margin → the ink really deviates from the key → error; middle → review («на проверку», always drawn); low → the ink
supports the key → our misread → ok (mark removed).

Thresholds are MEASURED, not guessed (eval/runs/dictation/ctc_r5all_fused.json: 7,697 correct + 86 real misspellings,
all 60 sealed pages, design "judge only GLM-mismatch words"):
  τ_error 13.7 → ≈ 1.4 false red marks per 100 words (some are real errors our labels missed), catches ≈ 36 % of real ones
  τ_review 5.1 → review adds catch to ≈ 59 % at ≈ 3.7 review marks per 100 words; below → ok (≈ 60 % of false alarms removed)
Known limit: words the reader auto-corrected to the key never reach the judge (26 % of real misspellings) → R5b.
Preprocessing = the pipeline's own (BGR, h 64, w ≤ 512, zero pad); crops = ink box grown by bilimai.detector.WORD_GROW
(the CTC reader wants annotator-like word boxes — 2026-08-19 sweep). PMIWordVerifier + FusedWordVerifier below: the product
default is the fused judge (CTC top-20 → PMI, verdict from min of standardised margins — both judges must agree).

2026-08-24 — v6 IS NOW THE DEFAULT. The fused judge adds `isolation` (top1 − top20: is the winner a
SPIKE or a PLATEAU?), the pupil/reader `edit prior`, and per-writer `page_normalise`. Measured on the
population production actually judges (reader-mismatch words), held out both directions at a matched
0.64 false red marks per 100 words:

    v5  min(z_ctc, z_pmi)                        79/200 = 39.5 %
    v6  + isolation + edit prior + page norm    109/200 = 54.5 %

⚠ `judge()` therefore scores a PAGE AT A TIME — `page_normalise` needs the page's own words to judge
each child against their own hand. Feed it one page per call, as bilimai/dictation.py does.
⚠ Constants and the frozen pupil/reader models are GENERATED by eval/dictation/refit_verifier_v6.py.
Never edit them here. If either data file is missing the verifier prints a warning and falls back to
v5 — it never silently degrades.
⚠ The 54.5 % denominator is 200, not 261: design B never sees words the reader read as the key.
That architectural ceiling costs 61 of 261 real errors and is not a scorer problem.
"""
from __future__ import annotations
from pathlib import Path
import numpy as np

ROOT = Path(__file__).resolve().parents[1]
DEFAULT_OCR = ROOT / "models/readingpipeline/ocr"
TAU_ERROR, TAU_REVIEW = 13.7, 5.1


def execution_providers(available: list[str], coreml: bool | None):
    """Select an accelerator automatically while preserving the explicit CPU/CoreML controls."""
    if coreml is False:
        return ["CPUExecutionProvider"]
    if coreml is None and "CUDAExecutionProvider" in available:
        return ["CUDAExecutionProvider", "CPUExecutionProvider"]
    if "CoreMLExecutionProvider" in available and coreml is not False:
        return [
            ("CoreMLExecutionProvider", {"MLComputeUnits": "ALL", "ModelFormat": "MLProgram"}),
            "CPUExecutionProvider",
        ]
    return ["CPUExecutionProvider"]
class CTCWordVerifier:
    name = "ctc-word-verifier@rp-notebooks"

    def __init__(self, model_dir: str | Path = DEFAULT_OCR, tau_error: float = TAU_ERROR, tau_review: float = TAU_REVIEW,
                 threads: int = 8, max_cands: int = 0, batch: int = 64, coreml: bool | None = None):
        """ACCELERATOR BY DEFAULT (founder, 2026-09-11: "never run such tasks on cpu ... ml/ai inferences, trainings are
        always meant to be run on gpus"). coreml=None auto-selects CUDA on a GPU server or Apple's GPU/Neural Engine when
        available; pass coreml=False only to force the CPU fallback deliberately, and say why. Measured 2026-08-19:
        5.7 ms/crop on CoreML vs 190 ms on one CPU thread, max |Δlogprob| 2e-5; 2026-09-11 on the T09C probe, 219.8 s
        vs 1548.3 s per pass (7.0x) with 0/263 words or decodes changed between providers. The product target is our own
        GPU server (AGENTS.md: deployment is server-only).
        ⚠ NEVER MIX PROVIDERS INSIDE ONE COMPARISON: the provider is stamped, and a provider change reads as a model
        change. Repeatability means bit-identity WITHIN a provider."""
        import json, onnxruntime as ort
        cfg = json.load(open(Path(model_dir) / "ocr_config.json", encoding="utf-8"))
        self.alpha = cfg["alphabet"]; self.H, self.W = cfg["image"]["height"], cfg["image"]["width"]
        self.ch = {c: i + 2 for i, c in enumerate(self.alpha)}; self.BLANK, self.OOV = 0, 1
        so = ort.SessionOptions(); so.intra_op_num_threads = threads; so.inter_op_num_threads = threads; so.log_severity_level = 3
        providers = execution_providers(ort.get_available_providers(), coreml)
        self.sess = ort.InferenceSession(str(Path(model_dir) / "ocr_model.onnx"), so, providers=providers)
        self.inp = self.sess.get_inputs()[0].name
        self.tau_error, self.tau_review, self.max_cands, self.batch = tau_error, tau_review, max_cands, batch

    # ---- image side ---------------------------------------------------------------------------------------------------
    def _crop_box(self, img_bgr, box):
        from .detector import RPDetector, WORD_GROW
        H, W = img_bgr.shape[:2]; b = RPDetector._grow([float(v) for v in box], WORD_GROW, W, H)
        x0, y0, x1, y1 = [int(round(v)) for v in b]; x0, y0 = max(0, x0), max(0, y0); x1, y1 = max(x0 + 1, x1), max(y0 + 1, y1)
        return (x0, y0, x1, y1)

    def _crop(self, img_bgr, box):
        x0, y0, x1, y1 = self._crop_box(img_bgr, box); return img_bgr[y0:y1, x0:x1]

    # ---- letter geometry (CTC forced alignment) ----------------------------------------------------------------------
    def char_frames(self, lp, string):
        """Viterbi CTC alignment of `string` on the (T, C) log-prob matrix → per character the [first, last] frame it owns."""
        ids = self._encode(string); n = len(ids)
        if n == 0: return []
        ext = [self.BLANK]; [ext.extend([i, self.BLANK]) for i in ids]; S = len(ext); T = lp.shape[0]
        NEG = -1e9; D = np.full((T, S), NEG); B = np.zeros((T, S), dtype=np.int8)
        D[0, 0] = lp[0, ext[0]]
        if S > 1: D[0, 1] = lp[0, ext[1]]
        for t in range(1, T):
            for s_ in range(S):
                best, arg = D[t - 1, s_], 0
                if s_ >= 1 and D[t - 1, s_ - 1] > best: best, arg = D[t - 1, s_ - 1], 1
                if s_ >= 2 and ext[s_] != self.BLANK and ext[s_] != ext[s_ - 2] and D[t - 1, s_ - 2] > best: best, arg = D[t - 1, s_ - 2], 2
                D[t, s_] = best + lp[t, ext[s_]]; B[t, s_] = arg
        s_ = S - 1 if S == 1 or D[T - 1, S - 1] >= D[T - 1, S - 2] else S - 2
        path = [0] * T
        for t in range(T - 1, -1, -1):
            path[t] = s_; s_ -= B[t, s_]
        frames = [[None, None] for _ in ids]
        for t, st in enumerate(path):
            if st % 2 == 1:
                k = st // 2
                if frames[k][0] is None: frames[k][0] = t
                frames[k][1] = t
        return frames

    def letter_geometry(self, lp, ink, key, crop_box, crop_w):
        """Which letters of the ink differ from the key, with page-pixel boxes. ink = the spelling the ink supports (the judge's
        best candidate), key = the expected word. Frames → crop x: the crop was resized to height H (width nw ≤ W=512), the
        network emits T frames over the 512-px canvas (8 px per frame) → x_crop = frame·(512/T)·(crop_w/nw)."""
        import difflib
        x0, y0, x1, y1 = crop_box; T = lp.shape[0]; h = y1 - y0
        nw = min(int(crop_w * (self.H / max(1, h))), self.W); px_per_frame = (self.W / T) * (crop_w / max(1, nw))
        fr = self.char_frames(lp, ink)
        # CTC fires each letter on ~1 frame near the letter's START and blanks elsewhere → a letter's visual span runs from
        # its own firing to the next letter's firing (half a frame earlier); inter-firing distances track letter widths
        # (checked on «слушат» 2026-08-19: ш 664–741 vs true 667–730 page px)
        n = len(fr); ink_frames = max(1, min(T, int(round(nw / (self.W / T)))))
        spans = []
        for i in range(n):
            f0, f1 = fr[i]
            if f0 is None: spans.append(None); continue
            next_start = next((fr[j][0] for j in range(i + 1, n) if fr[j][0] is not None), None)
            a = max(0.0, f0 - 0.5)
            b = (next_start - 0.5) if next_start is not None else min(ink_frames, f1 + 2.5)
            spans.append((a, max(b, a + 1)))
        def fbox(i):                                        # page box of ink letter i
            sp = spans[i]
            if sp is None: return None
            return [x0 + sp[0] * px_per_frame, y0, x0 + sp[1] * px_per_frame, y1]
        letters = []
        for tag, i1, i2, j1, j2 in difflib.SequenceMatcher(None, ink, key, autojunk=False).get_opcodes():
            if tag == "equal": continue
            if tag in ("replace", "delete"):
                bs = [b for b in (fbox(i) for i in range(i1, i2)) if b]
                if not bs: continue
                corr = key[j1:j2] if tag == "replace" else ""
                letters.append({"ink": ink[i1:i2], "correct": corr, "bbox": [min(b[0] for b in bs), y0, max(b[2] for b in bs), y1]})
            elif tag == "insert":                          # key letters missing in the ink → caret between ink letters
                left = fbox(i1 - 1) if i1 > 0 else None; right = fbox(i1) if i1 < len(ink) else None
                x = left[2] if left else (right[0] if right else x0); w = max(4.0, px_per_frame)
                letters.append({"ink": "", "correct": key[j1:j2], "bbox": [x - w / 2, y0, x + w / 2, y1]})
        return letters

    def _prep(self, crop):
        import cv2
        h, w = crop.shape[:2]; nw = min(int(w * (self.H / max(1, h))), self.W)
        im = cv2.resize(crop, (max(1, nw), self.H), interpolation=cv2.INTER_LINEAR)
        if nw < self.W: im = np.pad(im, ((0, 0), (0, self.W - nw), (0, 0)), "constant", constant_values=0)
        return np.moveaxis(im, -1, 0).astype(np.float32) / 255

    def logprobs(self, crops):
        """(T, C) log-softmax matrix per crop; batched ONNX."""
        out = []
        for s in range(0, len(crops), self.batch):
            x = np.stack([self._prep(c) for c in crops[s:s + self.batch]])
            y = self.sess.run(None, {self.inp: x})[0]                          # (T, N, C)
            if y.shape[1] != x.shape[0]: y = np.transpose(y, (1, 0, 2))
            out += [y[:, i] for i in range(y.shape[1])]
        return out

    # ---- scoring -----------------------------------------------------------------------------------------------------
    def _encode(self, s): return [self.ch.get(c, self.OOV) for c in s]

    def scores(self, lp, strings):
        """log P(s | crop) for each string: −ctc_loss, all strings in one call."""
        import torch
        T = lp.shape[0]; K = len(strings); tg = [self._encode(s) for s in strings]
        logp = torch.from_numpy(np.ascontiguousarray(lp)).unsqueeze(1).expand(T, K, lp.shape[1]).contiguous()
        tgt = torch.tensor([t for s in tg for t in s], dtype=torch.long)
        nll = torch.nn.functional.ctc_loss(logp, tgt, torch.full((K,), T, dtype=torch.long), torch.tensor([len(s) for s in tg]),
                                           blank=self.BLANK, reduction="none", zero_infinity=True)
        return (-nll).tolist()

    def verdict(self, margin: float) -> str:
        return "error" if margin >= self.tau_error else "review" if margin >= self.tau_review else "ok"

    def judge(self, img, items: list[dict]) -> list[dict]:
        """items: {"bbox": ink-tight word box (orig px), "key": key word, "read": reader's word or None}. Returns one dict
        per item: margin (best candidate − key, log-prob), best candidate, verdict. Batched: one ONNX pass for all words."""
        import cv2
        from .dictation import candidates
        if not items: return []
        if not isinstance(img, np.ndarray): img = cv2.cvtColor(np.asarray(img.convert("RGB")), cv2.COLOR_RGB2BGR)
        crops = [self._crop(img, it["bbox"]) for it in items]; lps = self.logprobs(crops); out = []
        for it, lp in zip(items, lps):
            key, read = it["key"], it.get("read"); cands = candidates(key, read, self.max_cands, wide=True)
            sc = self.scores(lp, [key] + cands); lk = sc[0]; lc = sc[1:]
            margin = (max(lc) - lk) if lc else 0.0; best = cands[int(np.argmax(lc))] if lc else None
            rec = {"margin": round(float(margin), 3), "best": best, "verdict": self.verdict(margin), "logp_key": round(float(lk), 3)}
            if rec["verdict"] == "error" and best:
                cb = self._crop_box(img, it["bbox"]); rec["letters"] = self.letter_geometry(lp, best, key, cb, cb[2] - cb[0])
            out.append(rec)
        return out


# ---------------------------------------------------------------------------------------------------------------- PMI
class PMIWordVerifier:
    """The GLM reader as a judge: score the key and candidate spellings in the read line's context, with and without the
    picture, and subtract the no-picture score (λ=0.5) so the reader's language habit cancels. Needs a GLMBatchReader
    (bilimai.reader) — the same loaded model the pipeline reads with. Items need line context: {"line_bbox", "line_words",
    "wi"} besides key/read. ~30 ms/word on a 5090, ~1.5 s/word on the Mac; used in cascade on the CTC top-K."""
    name = "pmi-word-verifier@glm"
    def __init__(self, reader, lam: float = 0.5, pad: int = 12): self.reader, self.lam, self.pad = reader, lam, pad
    def line_crop(self, img_pil, bbox):
        x0, y0, x1, y1 = bbox; W, H = img_pil.size
        return img_pil.crop((max(0, x0 - self.pad), max(0, y0 - self.pad), min(W, x1 + self.pad), min(H, y1 + self.pad)))
    def pmi(self, img_pil, item, strings):
        crop = self.line_crop(img_pil, item["line_bbox"])
        return self.reader.pmi_word_scores(crop, item["line_words"], item["wi"], strings, self.lam)


# -------------------------------------------------------------------------------------------------------------- fused
# z-standardisation constants and thresholds from the full-exam measurement — re-fit for the R5c adapter 2026-08-20
# (eval/runs/dictation/ctc_r5c_all_fused.json via eval/dictation/fuse_refit.py; v5 constants: Z_PMI (-2.059, 4.067),
# τ 2.29/0.74). fused = min(z_pmi, z_ctc) — "both judges must agree". Design B (judge reader-mismatch words only):
#   τ_error 2.30 → ≈ 0.65 false red / 100 words, catches ≈ 27 % of all real misspellings (36 % of judged)
#   τ_review 0.68 → catch incl. review ≈ 71 % of all (95 % of judged) at ≈ 6.5 marks / 100 words
#   ⚠ SUPERSEDED 2026-08-25: on the 275-label sealed set, 6.50 fp/100 gives 57.1 % and catch
#     SATURATES at 63.3 % at ANY budget (59 corrected away + 42 unscoreable can never be flagged).
#     The 71 % is the retired 200-judged/261-total population. See plans/AUDIT-2026-08-25-pipeline.md.
Z_PMI = (-2.095, 3.829); Z_CTC = (-5.413, 7.805); TAU_F_ERROR, TAU_F_REVIEW = 2.30, 0.68

# ------------------------------------------------------------------------------------------------ v6
# v6 adds two features and a per-writer term to the v5 fusion, measured 2026-08-24. Constants and the
# frozen pupil/reader edit models are GENERATED by eval/dictation/refit_verifier_v6.py — never edited
# here. Held out both directions on the population production actually judges (reader-mismatch words):
#   v5  min(z_ctc, z_pmi)                       79/200 = 39.5 %  at 0.64 false red /100 words
#   v6  + isolation + edit prior + page norm   109/200 = 54.5 %  at 0.64 false red /100 words
# If either data file is missing the verifier falls back to v5 and says so — it never silently
# degrades, because a silently different scorer is this project's most expensive failure mode.
# ---- READER SILOS (founder decision, 2026-08-25) -------------------------------------------------
# These constants and the fitted edit-prior models describe ONE READER'S MISTAKES. `reader_model()`
# is literally "the reader's confusion habits". Stored globally, refitting for Qwen would overwrite
# GLM's live production thresholds and a GLM run would then score against Qwen's numbers — silently,
# because nothing errors. So they are scoped per reader family.
#
# A family with no constants of its own MUST NOT inherit another's. Falling back loudly to v5 is
# acceptable; borrowing is not. GLM keeps the original unsuffixed filenames so the shipped reader is
# untouched by this change.
DEFAULT_FAMILY = "glm-ocr"
V6_CONST = ROOT / "bilimai/data/verifier_v6.json"          # legacy path == the GLM silo


class AdapterMismatch(Exception):
    """Constants fitted on one ADAPTER are being loaded for another.

    ⚠ MUST NOT be swallowed by the v5 fallback. Falling back to v5 on an adapter mismatch is not a
    graceful degradation — v5's own TAU constants were fitted on the same adapter, so the fallback
    is wrong in exactly the same way, and it happens silently. An adapter mismatch is a stop, not a
    downgrade. (Audit 2026-08-25: the first version of this check raised a bare AssertionError,
    which the blanket `except Exception` below caught and turned into a silent v5 fallback — the
    opposite of the advertised hard fail.)
    """


def _ad_slug(a) -> str:
    """Compare adapters by what DISTINGUISHES them, not by their mount path or family prefix.

    ⚠ MEASURED 2026-08-25. The same R5c adapter is written two ways in this repo:
        dumps / constants : '/kaggle/input/datasets/jahongir713/bilimai-glm-lora-ru-r5c'
        everywhere else   : 'models/adapters/glm-ocr-lora-ru-r5c'  (bilimai/pipeline.py default)
    A last-segment slug splits one adapter into two names and rejects the repo's own canonical
    spelling. Since FAMILY is already guarded separately, the adapter slug only has to discriminate
    WITHIN a family — so family-ish tokens are dropped and the distinguishing tail is compared.
    Both spellings above reduce to **'ru-r5c'** ('lora' is in the drop set too).

    ⚠ KNOWN LIMITATION — IT CAN COLLIDE, and the drop set is why. Family-ish tokens are removed, so
    'bilimai-qwen3-vl-lora-ru-r7' and 'bilimai-glm-ocr-lora-ru-r7' BOTH reduce to 'ru-r7'. Inside
    `_load_v6` that is harmless: constants are routed by FAMILY first (`const_path`) and the declared
    family is asserted, so a Qwen adapter can never reach a GLM constants file. But
    `eval/dictation/score_ladder.py` imports this same function and compares `BILIMAI_ADAPTER` against
    a dump's stamp with **no family dimension**, where two same-round adapters from different families
    would compare equal. Not reachable with any adapter name on disk today, and Arm 1's own pair
    separates cleanly — recorded as a trap, not a live fault. If cross-family adapter names ever
    collide, compare (family, adapter) as a pair rather than widening this function.
    """
    tail = str(a).rstrip("/").split("/")[-1].strip().lower()
    drop = {"bilimai", "glm", "ocr", "qwen", "qwen3", "vl", "lora"}
    parts = [t for t in tail.replace("_", "-").split("-") if t and t not in drop]
    slug = "-".join(parts)
    if not slug:
        # Every token was family-ish ('glm-ocr-lora', 'out/lora'). Two unrelated adapters would then
        # compare EQUAL, which is the one collision that is silent AND certain. Refuse instead.
        raise ValueError(
            f"adapter name {a!r} carries no distinguishing token — every part is a family/vendor word "
            f"({sorted(drop)}). Two such names would compare equal. Rename the adapter so it says "
            f"which round or arm it is (e.g. '...-r5c', '...-arm1').")
    return slug


def _slug(family: str) -> str:
    """Filesystem-safe family id. Never let a family name escape bilimai/data/."""
    return "".join(ch if (ch.isalnum() or ch in "-_") else "-" for ch in (family or "")).strip("-").lower()


def const_path(family: str = DEFAULT_FAMILY) -> Path:
    """Where this family's fused-verifier constants live."""
    return V6_CONST if _slug(family) == _slug(DEFAULT_FAMILY) else \
        ROOT / f"bilimai/data/verifier_v6_{_slug(family)}.json"


def edit_prior_path(family: str = DEFAULT_FAMILY) -> Path:
    """Where this family's fitted pupil/reader edit models live."""
    return ROOT / "bilimai/data/edit_prior_models.json" if _slug(family) == _slug(DEFAULT_FAMILY) else \
        ROOT / f"bilimai/data/edit_prior_models_{_slug(family)}.json"


def _load_v6(family: str = DEFAULT_FAMILY, adapter: str | None = None):
    """Load this family's fused-verifier constants.

    ⚠ THE SILO PROTECTS A FAMILY SWITCH, NOT AN ADAPTER SWITCH (audit 2026-08-25).
    `family` comes from the reader CLASS, so every GLM adapter — v5, R5c, and a letter-target Arm 1
    adapter whose whole purpose is to make DIFFERENT mistakes — reports `"glm-ocr"` and silently
    loads R5c's τ, gmed/gmad and fitted READER edit model. Nothing errors; the numbers are simply
    wrong, under the new adapter's name. `reader_model()` is literally "the reader's confusion
    habits", so constants fitted on one adapter describe another only by luck.
    See plans/CODE-DEFECTS-2026-08-25.md and plans/exec/2026-08-25-arm1-letter-targets.md §3b.

    So constants may ALSO stamp the adapter they were fitted on. Rules:
      * both sides declare an adapter and they differ  -> HARD FAIL (falls back to v5, loudly)
      * the constants carry no stamp                   -> warn; cannot be verified (legacy files)
      * no adapter passed                              -> unchanged behaviour
    `eval/dictation/refit_verifier_v6.py` stamps new constants, so the check tightens over time
    rather than breaking the shipped path today.
    """
    import json
    try:
        cp = const_path(family)
        c = json.loads(cp.read_text(encoding="utf-8"))
        declared = c.get("family", DEFAULT_FAMILY)
        assert _slug(declared) == _slug(family), \
            f"{cp.name} declares family {declared!r} but was loaded for {family!r} — refusing to borrow"
        stamped = c.get("adapter")
        if stamped and adapter and _ad_slug(stamped) != _ad_slug(adapter):
            raise AdapterMismatch(
                f"{cp.name} was fitted on adapter {stamped!r} but is being loaded for {adapter!r} "
                f"(slugs {_ad_slug(stamped)!r} vs {_ad_slug(adapter)!r}). These constants describe "
                "the OTHER adapter's mistakes — refusing to borrow, and refusing to fall back to v5, "
                "whose TAUs were fitted on the same adapter. Re-fit with "
                "eval/dictation/refit_verifier_v6.py --family/--adapter, or declare a distinct family.")
        if adapter and not stamped:
            print(f"[verifier] ⚠ {cp.name} carries no adapter stamp, so it cannot be checked against "
                  f"adapter {adapter!r}. Re-fit to stamp it. Audit 2026-08-25.")
        from .edit_prior import load_models
        pup, rdr = load_models(edit_prior_path(family))
        assert c["feature_order"] == ["ctc", "pmi", "iso", "lr"], "v6 feature order changed"
        return c, pup, rdr
    except AdapterMismatch:
        raise                                   # ⚠ a stop, not a downgrade — see AdapterMismatch
    except Exception as e:                      # noqa: BLE001 — any other failure means "use v5"
        print(f"[verifier] v6 unavailable for family {family!r} ({type(e).__name__}: {e}); "
              f"using v5 min(z_ctc, z_pmi). ⚠ v5's TAU constants were also fitted on {DEFAULT_FAMILY} — "
              "they are NOT calibrated for another reader.")
        return None, None, None


class FusedWordVerifier:
    """CTC judge on all ~500 spellings → its top-K go to the PMI judge → fused verdict.

    v6 scores a PAGE AT A TIME: `page_normalise` needs the page's own words to judge each child
    against their own hand, so `judge()` collects features for every item first and decides verdicts
    in a second pass. Falls back to the CTC verdict for an item without line context.
    """
    name = "fused-ctc+pmi-word-verifier"
    def __init__(self, ctc: CTCWordVerifier, pmi: PMIWordVerifier, topk: int = 20, tau_error=None, tau_review=None,
                 family: str | None = None, adapter: str | None = None):
        self.ctc, self.pmi, self.topk = ctc, pmi, topk
        # family = which READER produced the transcript these constants were fitted against.
        self.family = family or getattr(getattr(pmi, "reader", None), "family", None) or DEFAULT_FAMILY
        # ⚠ ADAPTER CHECKING IS EXPLICIT OPT-IN, and deliberately NOT derived from the reader.
        # Deriving it looked helpful and was wrong twice over: (a) the shipped constants carry no
        # stamp — they are GENERATED and must never be hand-edited (see this file's header) — so
        # every production construction would print an un-actionable warning; (b) a warning that
        # fires on every page is one nobody reads, which is worse than no warning. Pass `adapter=`
        # when you want the check; pass nothing and behaviour is exactly as before the audit.
        # The next `refit_verifier_v6.py --adapter ...` stamps the constants, at which point a
        # mismatch becomes a hard AdapterMismatch rather than a warning.
        self.adapter = adapter
        self.v6, self.pup, self.rdr = _load_v6(self.family, self.adapter)
        if self.v6:
            self.name = "fused-ctc+pmi+editprior-word-verifier@v6"
            self.tau_error = self.v6["tau_error"] if tau_error is None else tau_error
            self.tau_review = self.v6["tau_review"] if tau_review is None else tau_review
        else:
            self.tau_error = TAU_F_ERROR if tau_error is None else tau_error
            self.tau_review = TAU_F_REVIEW if tau_review is None else tau_review
    def verdict(self, fused): return "error" if fused >= self.tau_error else "review" if fused >= self.tau_review else "ok"
    def judge(self, img, items):
        import cv2
        from .dictation import candidates
        if not items: return []
        pil = img if not isinstance(img, np.ndarray) else None
        bgr = img if isinstance(img, np.ndarray) else cv2.cvtColor(np.asarray(img.convert("RGB")), cv2.COLOR_RGB2BGR)
        crops = [self.ctc._crop(bgr, it["bbox"]) for it in items]; lps = self.ctc.logprobs(crops)
        out = [None] * len(items); feats = []          # feats: (index, [ctc, pmi, iso, lr])
        # ---- pass 1: features per item. NO verdict yet — page_normalise needs the whole page. ----
        for n, (it, lp) in enumerate(zip(items, lps)):
            key, read = it["key"], it.get("read"); cands = candidates(key, read, 0, wide=True)
            sc = self.ctc.scores(lp, [key] + cands); lk = sc[0]; lc = sc[1:]
            ctc_margin = (max(lc) - lk) if lc else 0.0
            order = sorted(range(len(cands)), key=lambda i: -lc[i])[:self.topk]; top = [cands[i] for i in order]
            rec = {"ctc_margin": round(float(ctc_margin), 3), "best": cands[order[0]] if order else None}
            out[n] = rec
            if pil is not None and it.get("line_words") and it.get("line_bbox") is not None and it.get("wi") is not None and top:
                try:
                    ps = self.pmi.pmi(pil, it, [key] + top); pmi_margin = max(ps[1:]) - ps[0]
                    rec["pmi_margin"] = round(float(pmi_margin), 3)
                    if self.v6:
                        from .edit_prior import feature_row
                        # ⚠ isolation indexes rank 20 POSITIONALLY — the list must be sorted descending.
                        feats.append((n, feature_row(ctc_margin, pmi_margin, sorted(lc, reverse=True),
                                                     key, rec["best"], self.pup, self.rdr)))
                    else:
                        zc = (ctc_margin - Z_CTC[0]) / Z_CTC[1]; zp = (pmi_margin - Z_PMI[0]) / Z_PMI[1]
                        feats.append((n, None)); rec["fused"] = round(float(min(zc, zp)), 3)
                    continue
                except Exception as e:
                    rec["pmi_error"] = str(e)[:120]
            # no line context (or PMI failed) → CTC-only verdict, exactly as before
            rec.update({"verdict": self.ctc.verdict(ctc_margin), "margin": rec["ctc_margin"]})

        # ---- pass 2: page-level normalisation, then verdicts ----
        if feats and self.v6:
            from .edit_prior import fused_score
            idx = [n for n, _ in feats]
            X = [f for _, f in feats]
            # One judge() call is ONE page, so every row shares a page id. With fewer than
            # page_normalise's min_words the per-writer term falls back to the global standardiser —
            # the same behaviour the constants were fitted under (median ~11 judged words per page).
            s = fused_score(X, ["page"] * len(X), self.v6["gmed"], self.v6["gmad"])
            for n, fused in zip(idx, s):
                out[n].update({"fused": round(float(fused), 3), "verdict": self.verdict(float(fused)),
                               "margin": round(float(fused), 3)})
        elif feats:
            for n, _ in feats:
                f = out[n]["fused"]; out[n].update({"verdict": self.verdict(f), "margin": f})

        for n, it in enumerate(items):
            rec = out[n]
            if rec.get("verdict") == "error" and rec.get("best"):
                cb = self.ctc._crop_box(bgr, it["bbox"])
                rec["letters"] = self.ctc.letter_geometry(lps[n], rec["best"], it["key"], cb, cb[2] - cb[0])
        return out

